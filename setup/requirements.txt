# Runtime dependencies for Kairos-2, installed via pip into a venv (no Conda).
# Mirrors setup/environment.yml's runtime section, minus dev/test/build-only
# tooling (pytest, flake8, coverage, pre-commit, conda-build, aioresponses,
# aiounittest — none of these are imported by the running bot) and packages
# tied to connectors this fork doesn't ship (TA-Lib, web3, xrpl-py,
# injective-py, bip-utils, safe-pysha3, scalecodec, and the pip-only
# decibel-python-sdk/aptos-sdk/solders/lighter-sdk/eip712-structs).
# numba/llvmlite (pandas-ta) and eth-keyfile (eth-account) are pulled in
# transitively — not listed explicitly, same as the conda env.
aiohttp>=3.8.5,<3.14
asyncssh>=2.13.2
aioprocessing>=2.0.1
async-timeout>=4.0.2,<5
bidict>=0.22.1
Brotli>=1.2.0
cachetools>=5.3.1
cryptography>=41.0.2
eth-account>=0.13.0
numpy>=2.2.6
pandas>=2.3.2
pandas-ta>=0.4.71b
prompt_toolkit>=3.0.39
psutil>=5.9.5
ptpython>3.0.25
pydantic>=2
pyperclip>=1.8.2
requests>=2.31.0
ruamel.yaml>=0.2.5
scipy>=1.11.1
six>=1.16.0
base58>=2.1.1
sqlalchemy>=1.4.49
tabulate==0.9.0
tqdm>=4.67.1
typer>=0.9.0
ujson>=5.7.0
urllib3>=1.26.15,<2.0
PyYAML>=6.0
aiomqtt>=2.0.0
