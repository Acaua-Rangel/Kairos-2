#!/usr/bin/env python
"""Launcher for the `hbot` CLI.

Resolved on PATH inside the `.venv` created by `make install` (symlinked into the venv's bin
directory). Uses `env python` so it runs against whichever environment is active, with the repo
on sys.path via bin/path_util.py.
"""
import sys

import path_util  # noqa: F401  (puts the repo root ahead of bin/ on sys.path)

from kairos.cli.main import main

if __name__ == "__main__":
    sys.exit(main())
